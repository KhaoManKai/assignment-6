<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body tabindex="-1">
    <%- include('partials/navbar', {page: '/register'}) %>
    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold">Register</h1>
                    <p class="py-6">Please provide your credentials below</p>
                </div>
            </div>
        </div>
        <br />
        <div class="flex justify-center">
            <div class="w-full max-w-md">
                <% if(successMessage) { %>
                    <div class="alert mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <span><%= successMessage %></span>
                    </div>
                    <a href="/login" class="btn btn-success w-full">Proceed to Log in</a>
                <% } else { %>
                    <form method="POST" action="/register">
                        <div class="mb-4">
                            <label for="userName" class="block font-medium mb-2">User Name</label>
                            <input type="text" class="input input-bordered w-full" id="userName" name="userName" required value="<%= userName %>">
                        </div>
                        <div class="mb-4">
                            <label for="email" class="block font-medium mb-2">Email Address</label>
                            <input type="email" class="input input-bordered w-full" id="email" name="email" required>
                        </div>
                        <div class="mb-4">
                            <label for="password" class="block font-medium mb-2">Password</label>
                            <input type="password" class="input input-bordered w-full" id="password" name="password" required>
                        </div>
                        <div class="mb-6">
                            <label for="password2" class="block font-medium mb-2">Confirm Password</label>
                            <input type="password" class="input input-bordered w-full" id="password2" name="password2" required>
                        </div>
                        <% if(errorMessage) { %>
                            <div class="alert alert-error mb-4">
                                <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                <span><%= errorMessage %></span>
                            </div>
                        <% } %>
                        <button type="submit" class="btn btn-primary w-full">Register</button>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>